node_modules/
dist/
build/
.env
.env.*
!.env.example
*.log
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editors
.vscode/
.idea/

# Working docs (active plan / current status) — not the durable record
docs/PLAN.md
docs/STATUS.md
docs/UI-ideas.md

# Private/exploratory design docs — not the durable record, not for sharing
docs/private-docs/

# Sandbox: throwaway experiments, never part of the repo (see sandbox/README.md)
sandbox/


# Drizzle migrations output is committed; local drizzle-kit cache is not
.drizzle/

# Uploaded document files — see backend/src/shared/storage.ts
backend/data/
